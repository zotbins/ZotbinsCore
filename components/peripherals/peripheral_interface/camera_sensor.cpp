#include "camera_sensor.hpp"
#include "esp_log.h"

static const char* TAG = "camera_sensor";

esp_err_t init_camera(void) {
    ESP_LOGI(TAG, "Camera initialization not yet implemented.");
    return ESP_OK;
}

